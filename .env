# Bind dev server (optional)
HOST=localhost
# CRA 5 bug: khi vừa dùng proxy vừa set HOST thì allowedHosts bị undefined → bật tắt kiểm tra host (chỉ dùng khi dev)
DANGEROUSLY_DISABLE_HOST_CHECK=true
# Backend API (dev dùng proxy nên không bắt buộc)
REACT_APP_API_URL=http://localhost:8081
