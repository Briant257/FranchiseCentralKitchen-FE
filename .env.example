# Dev server host (optional)
HOST=localhost
# Cần khi dùng cả HOST + proxy (CRA 5): tắt host check để tránh lỗi allowedHosts
DANGEROUSLY_DISABLE_HOST_CHECK=true
# Backend Central Kitchen API (bắt buộc cho login + token)
REACT_APP_API_URL=http://localhost:8081
